# 추후 개선사항 — 브레인스토밍

**Date**: 2026-02-22  
**Author**: PDCA / deep  
**Project**: Mendly (LifeBalanceAI)  
**관련**: real-device-feedback-round3.plan.md, improvement-ideas-brainstorm.plan.md, empty-states-consistency.plan.md

---

## 1. 해석 (의도)

| 표면 | 실제 의도 |
|------|-----------|
| "추후 개선사항 뭐있을까? 브레인스토밍 후 적용" | **(1)** 실기기 피드백·기존 계획을 바탕으로 **추후 할 일 목록**을 체계적으로 정리하고, **(2)** 그중 **즉시 적용 가능한 항목**은 코드에 반영한다. |

---

## 2. 미완·보류 (실기기 피드백 3차 기준)

| # | 항목 | 상태 | 비고 |
|---|------|------|------|
| **2** | TO-DO 반복 "매주 O 요일마다" | 보류 | 번거로우면 패스. 현재 매일/매주/매월만 지원. |
| **4-2** | 일정 반복 (매일/매주/매월) | 미구현 | events 테이블 recurrence 스키마·UI design 후 Do. |
| **7** | 리뷰 목표·성취도 연동 | 미구현 | 일정/할일 성취도 필드 + 캘린더 설정 + 리뷰 집계. plan → design 필요. |

---

## 3. UX·일관성 (Quick 적용 후보)

| # | 항목 | 설명 | 적용 |
|---|------|------|------|
| U1 | **EmptyState 다크모드** | 모든 탭 빈 상태에서 `dark={theme === 'dark'}` 전달해 다크에서도 제목·설명 가독성 확보. | ✅ 적용 |
| U2 | **시간 선택 모달 접근성** | 시간 피커 모달 "완료" 버튼에 accessibilityLabel. | 이미 t.common.done 사용 |
| U3 | **Pull-to-refresh 힌트** | 캘린더·리뷰 등에서 당기면 새로고침된다는 문구(빈 상태 또는 첫 진입). | 선택 |
| U4 | **에러 시 재시도 버튼** | API 실패 시 Alert만이 아니라 화면 내 "다시 시도" 버튼 노출. | 부분 적용됨(캡처 등) |

---

## 4. 기능 확장 (Design 후 Do)

| # | 항목 | 설명 | 참고 |
|---|------|------|------|
| F1 | **일정 반복** | 일정 추가/편집 시 반복 규칙(없음/매일/매주/매월) 선택. | round3 §4-2 |
| F2 | **성취도·리뷰 연동** | 일정/할일 단위 성취도 설정 → 리뷰 탭에서 집계·표시. | round3 §7 |
| F3 | **TO-DO 매주 O요일** | 반복 옵션에 "매주 월/화/…" 선택. | 번거로우면 패스 |
| F4 | **Schedule 결과 수정·재배치** | 생성된 주간 일정에서 활동별 시간 변경 후 AI 재배치. | improvement-ideas F3 |
| F5 | **스마트 캘린더 제안** | "이 시간 비어 있음 → 이 목표 넣을까요?" 등. | smart-calendar plan |

---

## 5. 디자인·접근성

| # | 항목 | 설명 | 참고 |
|---|------|------|------|
| D1 | **다크모드 전반 재점검** | 노트 미리보기·버튼 등 검정 글씨 잔여 구간 인라인 색 적용. | round3 §9 반영 후 추가 스캔 |
| D2 | **접근성 감사** | 포커스 순서, 스크린 리더, 터치 타겟 48dp 체크리스트. | improvement-ideas D5 |
| D3 | **로딩 스켈레톤** | 긴 대기 시 스피너 대신 스켈레톤(리뷰, 스케줄 결과 등). | improvement-ideas D3 |

---

## 6. 품질·운영

| # | 항목 | 설명 |
|---|------|------|
| Q1 | **에러 모니터링** | Sentry 등 연동, AI 호출 실패·한도 로깅. |
| Q2 | **E2E/실기기 회귀** | 일정 추가(시간 선택 모달), TO-DO 마감일, 리뷰 게이지 등 핵심 플로우 체크리스트. |
| Q3 | **오프라인 큐 UI** | 오프라인 시 저장 큐 표시, 동기화 중 인디케이터. |

---

## 7. 우선 적용 (이번 사이클)

- **U1**: 모든 EmptyState에 `dark={theme === 'dark'}` 전달 → 다크모드 빈 상태 가독성 통일.
- (선택) U3, U4는 필요 시 다음 사이클에서.

---

## 8. 권장 다음 단계 (PDCA)

| 순서 | 항목 | 액션 |
|------|------|------|
| 1 | **일정 반복** | plan → design `event-recurrence.design.md` → Do |
| 2 | **성취도·리뷰 연동** | plan → design `achievement-goal-review.design.md` → Do |
| 3 | **다크모드 재점검** | 화면별 스캔 후 검정 글씨 잔여 구간 수정 |
| 4 | **Schedule 결과 수정** | schedule-edit design 참고 후 Do |

---

**Next**: U1 적용 후 tsc·lint 실행. 이후 "다음 개선"은 위 표 1~4 중에서 선택해 pdca plan/design 진행.
